version: '3.8'

services:
  python-academy:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=super-secret-key-change-in-production-38291
      - DATABASE_URL=sqlite:///./python_academy.db
    volumes:
      - pyacademy_data:/app/data

volumes:
  pyacademy_data:
